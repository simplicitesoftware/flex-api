/*
 * Simplicite(R) for Adobe Flex(R)
 * http://www.simplicite.fr
 */
package simplicite.ui {
	import simplicite.core.*;
	
	/**
	 * Simplicit&eacute;(R) core application session UI helper class - <b>EXPERIMENTAL</b>
	 */
    public class AppSessionHelper extends CommonHelper {
		private var a:AppSession;

		/**
		 * Constructor
		 */
		public function AppSessionHelper(a:AppSession) {
			this.a = a;
		}
	}
}
